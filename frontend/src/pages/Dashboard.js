import {Container} from 'react-bootstrap';

const Dashboard = () => {
    return (
        <Container>
            <h3 className="page-title">Dashboard</h3>

        </Container>
    );
};

export default Dashboard;