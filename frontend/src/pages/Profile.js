import {Container} from 'react-bootstrap';
import {ResetPassword} from "../components";

const Profile = () => {
    return (
        <Container>
            <ResetPassword/>

        </Container>
    );
};

export default Profile;